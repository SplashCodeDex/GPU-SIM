<UserControl x:Class="NvidiaControlPanel.Views.Manage3DSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:NvidiaControlPanel.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:Manage3DSettingsViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#008080"/> <!-- NVIDIA Teal-ish -->
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="BorderBrush" Value="#FFAAAAAA"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="I would like to use the following 3D settings:"
                   Foreground="#003399"
                   FontSize="14"
                   Margin="10,10,10,5"/>

        <!-- Tabs (Global vs Program) -->
        <TabControl Grid.Row="1" Margin="10,0,10,10" BorderThickness="0">
            <TabItem Header="Global Settings" IsSelected="True">
                <StackPanel Margin="0,10,0,0">
                    <TextBlock Text="Global Presets:" Margin="0,0,0,5"/>
                    <ComboBox SelectedIndex="0" Width="250" HorizontalAlignment="Left">
                        <ComboBoxItem Content="Base profile"/>
                    </ComboBox>
                </StackPanel>
            </TabItem>
            <TabItem Header="Program Settings">
                <TextBlock Text="(Coming Soon)" Margin="10"/>
            </TabItem>
        </TabControl>

        <!-- Settings List -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Settings}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#FFEEEEEE"
                  VerticalGridLinesBrush="Transparent"
                  BorderThickness="1"
                  BorderBrush="#FFCCCCCC"
                  Margin="10,0,10,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Feature" Binding="{Binding Name}" Width="2*" IsReadOnly="True" FontWeight="Bold"/>

                <DataGridTemplateColumn Header="Setting" Width="1.5*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Value}" VerticalAlignment="Center" Padding="2,0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding Options}"
                                      SelectedItem="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                      IsEditable="False"
                                      BorderThickness="0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Bottom Description Area -->
        <Border Grid.Row="3" BorderBrush="#FFCCCCCC" BorderThickness="1" Margin="10,0,10,10" Height="80">
            <ScrollViewer>
                <TextBlock Text="Description of the selected feature (coming soon)."
                           TextWrapping="Wrap"
                           Padding="5"/>
            </ScrollViewer>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,10">
            <Button Content="Restore" Command="{Binding RestoreDefaultsCommand}" Width="75" Margin="0,0,10,0"/>
            <Button Content="Apply" Command="{Binding ApplyCommand}" Width="75"/>
        </StackPanel>
    </Grid>
</UserControl>
