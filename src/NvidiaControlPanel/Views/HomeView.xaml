<UserControl x:Class="NvidiaControlPanel.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:NvidiaControlPanel.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:HomeViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="NVIDIA Control Panel"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="{StaticResource PrimaryTextBrush}"
                       Margin="0,0,0,20"/>

            <!-- Graphics Card Information -->
            <Border Background="White" BorderBrush="#D3D3D3" BorderThickness="1" CornerRadius="0" Padding="20" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Graphics Card Information"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource NvidiaGreenBrush}"
                                   Margin="0,0,0,15"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="GPU:" Grid.Row="0" Grid.Column="0" Margin="0,3" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding GpuName}" Grid.Row="0" Grid.Column="1" Margin="0,3"/>

                            <TextBlock Text="Driver Version:" Grid.Row="1" Grid.Column="0" Margin="0,3" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding DriverVersion}" Grid.Row="1" Grid.Column="1" Margin="0,3"/>

                            <TextBlock Text="Dedicated Video Memory:" Grid.Row="2" Grid.Column="0" Margin="0,3" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding VideoMemory}" Grid.Row="2" Grid.Column="1" Margin="0,3"/>

                            <TextBlock Text="Bus Support:" Grid.Row="3" Grid.Column="0" Margin="0,3" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding BusSupport}" Grid.Row="3" Grid.Column="1" Margin="0,3"/>

                            <TextBlock Text="BIOS Version:" Grid.Row="4" Grid.Column="0" Margin="0,3" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding BiosVersion}" Grid.Row="4" Grid.Column="1" Margin="0,3"/>

                            <TextBlock Text="DirectX Support:" Grid.Row="5" Grid.Column="0" Margin="0,3" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding DirectXSupport}" Grid.Row="5" Grid.Column="1" Margin="0,3"/>
                        </Grid>
                    </StackPanel>

                    <Image Grid.Column="1"
                           Source="pack://application:,,,/Assets/Images/image_GTX-1650.png"
                           Width="200"
                           VerticalAlignment="Top"
                           Margin="20,0,0,0"/>
                </Grid>
            </Border>

            <!-- Device Specification -->
            <Border Background="White" BorderBrush="#D3D3D3" BorderThickness="1" CornerRadius="0" Padding="20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Device Specification"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource NvidiaGreenBrush}"
                                   Margin="0,0,0,15"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Device name:" Grid.Row="0" Grid.Column="0" Margin="0,3" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding DeviceName}" Grid.Row="0" Grid.Column="1" Margin="0,3"/>

                            <TextBlock Text="Processor:" Grid.Row="1" Grid.Column="0" Margin="0,3" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Processor}" Grid.Row="1" Grid.Column="1" Margin="0,3"/>

                            <TextBlock Text="Installed RAM:" Grid.Row="2" Grid.Column="0" Margin="0,3" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding InstalledRam}" Grid.Row="2" Grid.Column="1" Margin="0,3"/>

                            <TextBlock Text="Device ID:" Grid.Row="3" Grid.Column="0" Margin="0,3" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding DeviceId}" Grid.Row="3" Grid.Column="1" Margin="0,3"/>

                            <TextBlock Text="System type:" Grid.Row="4" Grid.Column="0" Margin="0,3" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding SystemType}" Grid.Row="4" Grid.Column="1" Margin="0,3"/>
                        </Grid>
                    </StackPanel>

                    <Image Grid.Column="1"
                           Source="pack://application:,,,/Assets/Images/laptop-icon-png-image.png"
                           Width="200"
                           VerticalAlignment="Top"
                           Margin="20,0,0,0"/>
                </Grid>
            </Border>


        </StackPanel>
    </Grid>
</UserControl>
